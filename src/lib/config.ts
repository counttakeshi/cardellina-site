// TODO: point this at the deployed Cloudflare Worker once it exists (see
// /worker in the repo root and the plan's "Contact form + Resend worker" phase).
export const CONTACT_ENDPOINT = 'https://contact.cardellina.com/enquiry';
