export interface Partner {
	slug: string;
	name: string;
	logoUrl?: string;
	description: string;
	url?: string;
}

// TODO: replace with the real content from the /partner-projects export.
export const partners: Partner[] = [];
