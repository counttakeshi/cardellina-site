name = "cardellina-contact"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Custom domain routing (set up once the worker is deployed and DNS is ready):
# routes = [{ pattern = "contact.cardellina.com/*", custom_domain = true }]

# Set the real secret with:
#   npx wrangler secret put RESEND_API_KEY
# Set allowed origin(s) for CORS here or as another secret/var once the production
# domain is live, e.g.:
# [vars]
# ALLOWED_ORIGIN = "https://cardellina.com"
